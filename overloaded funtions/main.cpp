función find_max(nums) {
 sea max_num = Número. NEGATIVE_INFINITY; // más pequeño que todos los demás números
 para (sea número de números) {
  si (número > max_num) {
  num = max_num
 }
}
max_num de retorno;
 }